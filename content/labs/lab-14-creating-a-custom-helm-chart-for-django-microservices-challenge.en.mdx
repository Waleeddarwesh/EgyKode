---
labId: lab-14-creating-a-custom-helm-chart-for-django-microservices-challenge
title: "Creating a Custom Helm Chart for Django Microservices — Challenge"
description: "Turn a directory of manifests into a versioned chart you can install into any environment with different values."
domain: helm
level: intermediate
type: lab
phase: kubernetes
order: 14
tier: challenge
estimatedMinutes: 23
cloudCost: true
costTier: low
costEstimate: "**Depends on an existing cluster.** Helm itself is free; the workloads it installs consume cluster capacity."
cleanup:
  - "helm uninstall <release> -n <namespace>"
  - "kubectl get all -n <namespace>"
successCriteria:
  - "One chart installs into two environments with different values and no edited templates."
  - text: "`helm template` renders correct YAML before anything touches the cluster."
    verify: command
  - "A config change causes a rollout, rather than mutating Pods that never restart."
  - text: "`helm lint` and a rendered dry run both pass in CI."
    verify: command
guidedLabId: lab-14-creating-a-custom-helm-chart-for-django-microservices
authors: [waleed]
updated: 2026-08-09
---

## The goal

Build **Creating a Custom Helm Chart for Django Microservices** yourself, starting from an empty directory, without following the guided steps.

Everything you need is in the criteria above. Work down them one at a time, and verify each before moving to the next.

## Rules

- Do not open the guided lab until you have genuinely tried.
- When something fails, read the error before you search. The error message is the lesson.
- Tear down anything billable as soon as you finish.

## If you get stuck

The guided lab is one click away at the bottom of this page. Using it is not failure — coming back and repeating the challenge afterwards is the point.
