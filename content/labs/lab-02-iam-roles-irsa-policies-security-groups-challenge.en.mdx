---
labId: lab-02-iam-roles-irsa-policies-security-groups-challenge
title: "IAM Roles, IRSA Policies & Security Groups — Challenge"
description: "Give the cluster, the nodes and the build server exactly the permissions each needs and nothing more."
domain: terraform
level: intermediate
type: lab
phase: iac
order: 2
tier: challenge
estimatedMinutes: 15
cloudCost: true
costTier: low
costEstimate: "Free — IAM roles, policies and security groups cost nothing. Only the resources they are attached to do."
cleanup:
  - "terraform destroy -auto-approve"
  - "aws iam list-roles --query 'Roles[?starts_with(RoleName, `ivolve`)].RoleName'"
handsOn:
  online:
    platform: killercoda
    enabled: false
  cloud:
    enabled: true
    platform: aws
    requiresOwnAccount: true
successCriteria:
  - "Each role's trust policy names exactly one service, and its permissions policy names exactly the actions it needs."
  - "Security groups reference each other rather than CIDR ranges, so no rule contains a hardcoded IP."
  - "The database accepts connections only from the worker node security group — verified by trying from elsewhere."
  - "A pod assumes an IAM role through IRSA without any credential file existing."
guidedLabId: lab-02-iam-roles-irsa-policies-security-groups
authors: [waleed]
updated: 2026-08-09
---

## The goal

Build **IAM Roles, IRSA Policies & Security Groups** yourself, starting from an empty directory, without following the guided steps.

Everything you need is in the criteria above. Work down them one at a time, and verify each before moving to the next.

## Rules

- Do not open the guided lab until you have genuinely tried.
- When something fails, read the error before you search. The error message is the lesson.
- Tear down anything billable as soon as you finish.

## If you get stuck

The guided lab is one click away at the bottom of this page. Using it is not failure — coming back and repeating the challenge afterwards is the point.
